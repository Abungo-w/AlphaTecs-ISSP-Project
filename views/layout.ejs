<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AlphaTecs - Homepage</title>
    <link rel="stylesheet" href="/css/styles.css">

    <% if (typeof additionalCSS !== 'undefined') { %>
        <link rel="stylesheet" href="<%= additionalCSS %>.css">
    <% } %>

    <% if (typeof additionalCSS2 !== 'undefined') { %>
        <link rel="stylesheet" href="<%= additionalCSS2 %>.css">
    <% } %>
</head>
<body>
    <header>
        <div class="navbar">
            <div class="logo-section">
                <img src="/Images/logo.png" alt="AlphaTecs Logo" class="logo">
            </div>
            <div class="search-bar">
                <input type="text" placeholder="Search for coaching programs">
            </div>
            <div class="header-options">
                <% if (user && user.role === 'admin') { %>
                    <a href="/modules/create" class="nav-btn create-module-btn">Create Module</a>
                    <a href="/courses/create_course" class="nav-btn create-course-btn">Create Course</a>
                    <a href="/courses" class="nav-btn view-courses-btn">View Courses</a>
                    <a href="/modules" class="nav-btn view-modules-btn">View Modules</a>
                    <a href="/admin" class="nav-btn admin-panel-btn">Admin Panel</a>
                <% } %>
                <img src="/Images/notification.png" class="notification-icon">
                <div class="user-icon">                    
                    <% if (user) { %>
                    <%- user.name %>
                    <div class="dropdown">
                        <ul>
                            <li><a href="/goals">Goals</a></li>
                            <li><a href="/my-learning">My Learning</a></li>
                            <li><a href="/proposed-courses">Proposed Courses</a></li>
                            <li><a href="/accomplishments">Accomplishments</a></li>
                            <li><a href="/profile">Profile</a></li>
                            <li><a href="/help">Help</a></li>
                            <li><a href="/logout">Log Out</a></li>
                        </ul>
                    </div>
                <% } else { %>
                    <a href="/login">Login</a>
                <% } %></div>
            </div>
        </div>
    </header>
    <nav>
        <!-- ...existing nav items... -->
        <% if (user && user.isAdmin) { %>
            <a href="/modules" class="nav-link">Modules</a>
        <% } %>
        <!-- ...existing code... -->
    </nav>
    <main class="main-content">
        <%- body %>
    </main>
    <footer>
        <p>© 2025 AlphaTecs. All rights reserved.</p>
    </footer>
    <script src="/js/script.js"></script>
</body>
</html>